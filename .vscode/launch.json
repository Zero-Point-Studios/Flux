{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Launch Flux Engine (Auto-Build)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true, 
            "preLaunchTask": "CMake: build"
        }
    ]
}